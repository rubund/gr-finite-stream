# auto-generated by grc.converter

id: finite_stream_concatenate
label: Concatenate
category: '[finite_stream]'

parameters:
-   id: ninputs
    label: N input streams
    dtype: int
    default: '2'
    hide: part
-   id: type
    label: Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
    hide: part
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: tag_parts
    label: Tag parts
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }
    multiplicity: ${ ninputs }

outputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }
asserts:
- ${ vlen > 0 }

templates:
    imports: import finite_stream
    make: |-
        finite_stream.concatenate(${type.size}*${vlen}, ${ninputs})
        self.${id}.set_tag_parts(${tag_parts})
    callbacks:
    - self.${id}.set_tag_parts(${tag_parts})

file_format: 1
