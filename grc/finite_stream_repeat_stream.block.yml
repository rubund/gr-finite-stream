# auto-generated by grc.converter

id: finite_stream_repeat_stream
label: Repeat stream
category: '[finite_stream]'

parameters:
-   id: n_times
    label: Times to repeat
    dtype: int
    default: '0'
-   id: type
    label: Data Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
    hide: part
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: max_samples
    label: Max samples to repeat
    dtype: int
    default: '10000000'
-   id: add_repeat_tag
    label: Add repeat tag
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }
asserts:
- ${ vlen > 0 }

templates:
    imports: import finite_stream
    make: finite_stream.repeat_stream(${type.size}*${vlen}, ${n_times}, ${max_samples})

file_format: 1
